"""Notebook test entry point.

Notebooks are tested via nbmake. Run with:

```sh
pytest --nbmake examples/
```

This file exists so that pytest discovers the tests/ directory and the command
above can be used alongside the standard test suite.
"""
